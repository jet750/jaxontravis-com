# Drop portfolio images here (.jpg/.jpeg/.png/.webp/.avif).
# They are auto-discovered via import.meta.glob in src/data/photographyConfig.js —
# no manual registration needed. Add optional title/alt overrides in that file.
